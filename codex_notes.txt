[AI Travel Agent] Fase 1 - Dados de Destinos e Perfis
[AI Travel Agent] Fase 2 - Embeddings de Destinos
[AI Travel Agent] Fase 2 - RAG para Descoberta de Destinos
[AI Travel Agent] Fase 3 - Motor de Score de Viagem
[AI Travel Agent] Fase 4 - Raciocínio LLM para Planejamento
[AI Travel Agent] Fase 5 - Orquestrador de Plano de Viagem
[AI Travel Agent] Fase 5 - Refino do Orquestrador e Testes
[AI Travel Agent] Fase 6 - API FastAPI (Plano de Viagem)
[AI Travel Agent] Fase 6 - API (Infra, Erros e Testes)
[AI Travel Agent] Fase 7 - Frontend (Formulário de Viagem)
[AI Travel Agent] Fase 7 - Frontend (Roteiro e Comparação)
[AI Travel Agent] Fase 8 - Deploy da API em Produção
[AI Travel Agent] Fase 8 - Deploy do Frontend em Produção
[AI Travel Agent] Fase 9 - Hardening e Refatoração
[AI Travel Agent] Fase 9 - Documentação e Diagramas
[AI Travel Agent] Fase 9 - Observabilidade Mínima
[AI Travel Agent] Fase 10 - Demo e Empacotamento Final


1) Freeze the MVP scope (one page spec)
MVP objective: prove “dynamic points economics with financial-grade ledger” for 1 tenant.

Area	MVP (build now)	Not MVP (later)
Tenancy	1 tenant hardcoded config	multi-tenant isolation, billing
Ledger	double-entry ledger for points	complex IFRS pack, taxes pack
Earn	earn rule + dynamic multiplier	full ML optimization
Burn	burn quote pricing + reservation	full partner routing
Controls	liability ceiling + audit log	full risk scoring
Interfaces	REST API + simple admin	full UI, SSO, SCIM

Deliverable: API that can (1) credit points, (2) quote redemption price, (3) reserve, (4) confirm burn, (5) show real-time liability.

2) Architecture for the MVP (simple, correct)
Pattern: event-sourced ledger + deterministic pricing service.

Component	Purpose	Tech
API Gateway	REST endpoints	FastAPI
Ledger Service	source of truth	Postgres
Pricing Service	dynamic earn/burn quotes	Python module (later separate svc)
Risk/Controls	ceilings, rate limits	middleware + rules
Async Bus	publish events	Redis stream or Postgres outbox (start with outbox)

3) Start coding, but only after scaffolding the repo correctly
Repo layout
bash
Copy code
laas/
  app/
    main.py
    core/config.py
    db/session.py
    ledger/models.py
    ledger/service.py
    pricing/engine.py
    api/routes.py
  migrations/
  tests/
  docker-compose.yml
  pyproject.toml
  README.md
Data model (minimum viable)
You need accounts, journal entries, transactions, holds/reservations.

Table	Key fields	Why it exists
account	id, tenant_id, type(user/liability), owner_id	ledger accounts
entry	id, txn_id, account_id, amount, direction, ts	double-entry records
txn	id, type(earn/burn/expire/adjust), status, ts	transaction envelope
hold	id, user_account_id, points, expires_at, status	burn reservation

4) Minimal API surface (do not add more)
Endpoint	What it does
POST /earn	credits points via double-entry
POST /quote	returns burn price for a reward
POST /reserve	creates a hold for burn
POST /confirm_burn	finalizes burn (moves held points)
GET /balance/{user_id}	available + held
GET /liability	total outstanding points

5) Starter code (you can paste and run)
docker-compose.yml
yaml
Copy code
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: laas
      POSTGRES_PASSWORD: laas
      POSTGRES_DB: laas
    ports:
      - "5432:5432"
app/main.py
python
Copy code
from fastapi import FastAPI
from app.api.routes import router

app = FastAPI(title="LaaS MVP")
app.include_router(router)
app/api/routes.py
python
Copy code
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel, Field
from app.ledger.service import LedgerService
from app.pricing.engine import PricingEngine

router = APIRouter()
ledger = LedgerService()
pricing = PricingEngine()

class EarnIn(BaseModel):
    user_id: str
    base_points: int = Field(ge=1)
    reason: str = "purchase"

class QuoteIn(BaseModel):
    user_id: str
    reward_id: str
    list_price_points: int = Field(ge=1)

class ReserveIn(BaseModel):
    user_id: str
    points: int = Field(ge=1)

@router.post("/earn")
def earn(payload: EarnIn):
    txn = ledger.earn(user_id=payload.user_id, base_points=payload.base_points, reason=payload.reason)
    return {"txn_id": txn}

@router.post("/quote")
def quote(payload: QuoteIn):
    price = pricing.quote_burn_price(
        user_id=payload.user_id,
        reward_id=payload.reward_id,
        list_price_points=payload.list_price_points,
    )
    return {"reward_id": payload.reward_id, "price_points": price}

@router.post("/reserve")
def reserve(payload: ReserveIn):
    ok, hold_id = ledger.reserve(user_id=payload.user_id, points=payload.points)
    if not ok:
        raise HTTPException(status_code=400, detail="insufficient_points")
    return {"hold_id": hold_id}

@router.get("/balance/{user_id}")
def balance(user_id: str):
    return ledger.balance(user_id=user_id)

@router.get("/liability")
def liability():
    return ledger.liability()
app/pricing/engine.py (deterministic “dynamic” pricing, pre-ML)
python
Copy code
import math
from dataclasses import dataclass

@dataclass
class PricingEngine:
    # knobs you can evolve later into learned models
    peak_multiplier: float = 1.15
    vip_discount: float = 0.95
    floor: int = 1

    def quote_burn_price(self, user_id: str, reward_id: str, list_price_points: int) -> int:
        # placeholder segmentation (swap to real features later)
        is_vip = user_id.endswith("VIP")
        m = self.peak_multiplier * (self.vip_discount if is_vip else 1.0)

        price = math.ceil(list_price_points * m)
        return max(self.floor, price)
app/ledger/service.py (in-memory first; swap to Postgres next)
python
Copy code
import time
import uuid
from collections import defaultdict

class LedgerService:
    def __init__(self):
        self.available = defaultdict(int)
        self.held = defaultdict(int)
        self.holds = {}

    def earn(self, user_id: str, base_points: int, reason: str) -> str:
        # MVP: just credit. Next step is double-entry + db.
        self.available[user_id] += base_points
        return f"txn_{uuid.uuid4().hex}"

    def reserve(self, user_id: str, points: int):
        if self.available[user_id] < points:
            return False, None
        self.available[user_id] -= points
        self.held[user_id] += points

        hold_id = f"hold_{uuid.uuid4().hex}"
        self.holds[hold_id] = {"user_id": user_id, "points": points, "ts": int(time.time()), "status": "RESERVED"}
        return True, hold_id

    def balance(self, user_id: str):
        return {"user_id": user_id, "available": self.available[user_id], "held": self.held[user_id]}

    def liability(self):
        total = sum(self.available.values()) + sum(self.held.values())
        return {"total_points_liability": total}
Run:

bash
Copy code
pip install fastapi uvicorn pydantic
uvicorn app.main:app --reload
This is intentionally not enterprise-grade yet. It proves the flow fast.

6) Acceptance criteria (the “done” bar for the MVP slice)
Test	Expected
Earn 1000 points	balance available +1000
Quote reward 5000	returns price with multiplier
Reserve quoted price	available decreases, held increases
Liability reflects holds	total = available + held
Negative / over-reserve	rejected

7) Immediate next commit after this (non-negotiable)
Replace the in-memory ledger with Postgres + double-entry and add an outbox table for events. That’s where “financial-grade” begins.

If you do only one thing today: implement the vertical slice above, then lock the scope.